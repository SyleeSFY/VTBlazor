@using Client.Core.Entities.Enums
@using Client.Core.Features

<nav class="nav_bar">
    <img src="/image/Shared/nav_bar.png" alt="Логотип кафедры" class='nav_bar_IMG' />
    <nav class="nav_menu">
        <ul class="nav_li">

            <NavLink href="/" class="nav_link auth-link Cursor" style="text-decoration: none">
                <li class="nav_item Cursor">КАФЕДРА</li>
            </NavLink>

            @if (isAuth && role == (int)Role.student)
            {
                <NavLink href="TaskStudentTable" class="nav_link auth-link Cursor" style="text-decoration: none">
                    <p class="nav_item Cursor">ЗАДАНИЯ</p>
                </NavLink>
            }
            else if (isAuth && role == (int)Role.educator)
            {
                <NavLink href="TaskEducator" class="nav_link auth-link Cursor" style="margin-left: auto; text-decoration: none">
                    <p class="nav_item Cursor">ЗАДАНИЯ</p>
                </NavLink>
            }
            else if (isAuth && role == (int)Role.admin)
            {
                <NavLink href="UserTable" class="nav_link auth-link Cursor" style="text-decoration: none">
                    <p class="nav_item Cursor">ПОЛЬЗОВАТЕЛИ</p>
                </NavLink>
                <NavLink href="DiciplineTable" class="nav_link auth-link Cursor" style="text-decoration: none">
                    <p class="nav_item Cursor">ПРЕДМЕТЫ</p>
                </NavLink>
                <NavLink href="GroupTable" class="nav_link auth-link Cursor" style="text-decoration: none">
                    <p class="nav_item Cursor">ГРУППЫ</p>
                </NavLink>
            }
        </ul>
        <ButtonExit />
    </nav>
</nav>